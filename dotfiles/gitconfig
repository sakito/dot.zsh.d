[user]
    name = sakito
    email = sakito@sakito.com
[color]
    ui = true
[core]
    quotepath = false
[difftool "sourcetree"]
    cmd = /usr/local/bin/meld $LOCAL $REMOTE
    path = 
[mergetool "sourcetree"]
    cmd = /usr/local/bin/meld $LOCAL $BASE $REMOTE --auto-merge --output=$MERGED
    trustExitCode = true
[merge]
    ff = false
[pull]
    ff = only
[alias]
    st = status -s
    co = checkout
    ci = commit
    b = branch --no-merged

# global な gitignore は $HOME/.config/git/ignore に配置

[ghq]
    root = ~/src
